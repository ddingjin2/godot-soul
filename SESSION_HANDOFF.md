# 인수인계 — 2026-09-10

새 세션은 이 파일부터 읽는다. 그다음 `README.md`의 현재 상태, 그다음 `AGENTS.md`.

## 지금 어디까지 왔나

Unity 6 → Godot 4.7.2 이식이 끝났고, 옆 프로젝트(`C:\dev\project-godot`) 기준으로 품질을
끌어올리는 작업이 **대부분 끝났다.**

| 축 | 상태 |
|---|---|
| 이식 자체 | 완료. 스위트 206 passed / 1 failed / 1 skipped |
| 저장소 위생 | 완료 |
| 로컬라이제이션 | 완료. ko/en 34키 |
| 제작 규칙 감사 | 완료. 씬·UI 61건, 수치 약 530건 |
| **규칙 3 — UI는 씬에 배치** | **완결.** 제품 코드에 런타임 UI 조립 없음 |
| **규칙 2 — 재사용 단위는 씬** | **완결.** 씬 22개 + Theme 1개 |
| 규칙 1 — 수치 데이터화 | 진행 중. S1·S4·S5 완료, S6~S13 남음 |
| 규칙 4 — 일회성 테스트 격리 | 지켜지는 중. 모든 probe가 `Tests/` 밖에서 살고 죽었다 |

전환이 **실제 결함 6건**을 찾았다. 전부 `PORT_STATUS.md`에 있다.

정본 계획은 [docs/migrations/scene-data/PLAN.md](docs/migrations/scene-data/PLAN.md).

## 브랜치

`master`가 이식 본체, `refactor/godot-scene-data`가 전환 브랜치. **병합하지 않았고 push한 적도
없다.** 원격 없음. 커밋 23개, 각 커밋이 스위트 green으로 착지했다.

## 다음에 할 일

1. **수치 전환 S6~S13.** `AUDIT_NUMBERS.md` 순서대로. 전부 추가적이며 출하된 키의 값을 바꾸지
   않는다. 예외는 S10(`Readability.json` 재생성) 하나이고, 키만 늘었음을 diff로 증명해야 한다.
2. **`GameplayBuildShim` 퇴역.** 감사는 S8이 없앨 거라 봤지만 테스트가 아직 쓴다. 별도 단계가
   필요하다.
3. **S9 아레나 authoring은 일정에 없다.** 아레나를 씬에 넣으면 `SceneLayout_*.json`이 레이아웃
   정본 자리를 잃는다. 기획자 소유 데이터의 소유권이 옮겨가는 문제라 **물어본 뒤에** 한다.

## 사람이 봐야 하는 것 — 자동 검증으로 못 잡는다

1. **타이틀 설정 패널.** `CheckBox`는 `Button`이라, 공유 Theme이 붙으면서 옵션 상자가 테마 판
   위에 그려지고 hover/focus에 반응하게 됐다. 의도한 변경이고 되돌리는 법도 기록해뒀지만
   **아무도 눈으로 본 적이 없다.**
2. **공격 예고 맥동 결함.** `AttackReadout` 안의 `GameplayTelegraphPulse`가 렌더러를 못 찾아
   작동하지 않는다(이식 결함). 고치면 여섯 개가 눈에 띄게 뛰기 시작하므로 기록만 했다.
3. **재미 검증.** 자동 검증 통과와 다른 것이며 여전히 안 했다.

## 이번 세션에서 물린 것 — 같은 데서 또 미끄러지지 말 것

- **`run-tests.ps1`이 PowerShell 5.1에서 죽었다.** 네이티브 stderr 한 줄이 `ErrorRecord`가 되고
  `ErrorActionPreference = 'Stop'`이 그걸 치명적 오류로 만든다. 고쳤다.
- **테스트를 동시에 돌리면 안 된다.** 락은 없지만 `user://playerprefs.cfg`가 공유 파일이라
  저장을 건드리는 스위트끼리 픽스처를 무너뜨린다. 처음에 문서 네 곳에 "동시 실행 가능"이라고
  썼다가 전부 고쳤다. 오류가 아니라 **그럴듯한 숫자**가 나오는 실패라 특히 위험하다.
- **읽기 전용 감사 에이전트가 다른 에이전트의 정당한 작업을 침범으로 오인해 되돌렸다.**
  복구했지만 시간을 날렸다. 병렬로 돌릴 때는 각 담당에게 **소유 파일 목록을 명시**한다.
- **공유 빌드가 red면 나머지가 전부 막힌다.** 시그니처를 바꾸는 중간 상태를 트리에 오래 두지
  않는다.

## 확인하지 않은 것

- 육안 UI 확인 전반. headless 실행은 구조 검증이지 화면 확인이 아니다.
- 배포. Windows export template이 없어 EXE를 만든 적이 없다. `export_presets.cfg`는 있다.
- 오디오. `AudioFeedback`은 겹치는 큐를 끊는다. 귀로 확인한 적 없다.
